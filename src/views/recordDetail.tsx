import React from 'react';
import Layout from '../components/layout';

export const RecordDetail: React.FC = () => {
  return <Layout hasBack={false} title="record detail">
    record detail
  </Layout>
};
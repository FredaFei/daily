import React from 'react';
import Layout from '../components/layout';

export const RecordCreate: React.FC = () => {
  return <Layout hasBack={false} title="record edit">
    record RecordCreate
  </Layout>
};